# Copy this file to .env and fill in your own value. NEVER commit your real .env.

# Free Pinata account -> Developers -> API Keys -> New Key (scope: pinFileToIPFS +
# pinJSONToIPFS only) -> paste the JWT it gives you here.
# See the comment at the top of src/utils/ipfs.js for full setup steps.
VITE_PINATA_JWT=
